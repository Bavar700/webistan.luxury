import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        silver: {
          100: "#F5F5F7",
          200: "#EBEBF0",
          300: "#D1D1D6",
          400: "#8E8E93"
        },
        obsidian: {
          DEFAULT: "#0A0A0B",
          light: "#1C1C1E",
          dark: "#010101"
        },
        gold: {
          DEFAULT: "#D4AF37",
          muted: "#A68D56",
          bright: "#FFD700",
          soft: "rgba(212, 175, 55, 0.08)"
        },
        accent: {
          blue: "#007AFF",
          gold: "#D4AF37"
        }
      },
      fontFamily: {
        sans: ["var(--font-inter)", "system-ui", "sans-serif"],
        serif: ["var(--font-playfair)", "serif"],
        mono: ["SF Mono", "Menlo", "monospace"]
      },
      borderWidth: {
        "0.5": "0.5px"
      },
      boxShadow: {
        "glass-white": "0 8px 32px 0 rgba(255, 255, 255, 0.05)",
        "glass-dark": "0 8px 32px 0 rgba(0, 0, 0, 0.4)",
        "tech-glow": "0 0 20px rgba(212, 175, 55, 0.15)",
        "soft-float": "0 20px 40px rgba(0, 0, 0, 0.1)"
      },
      backgroundImage: {
        "silver-gradient": "linear-gradient(135deg, #F5F5F7 0%, #EBEBF0 100%)",
        "dark-glass": "linear-gradient(180deg, rgba(28, 28, 30, 0.8) 0%, rgba(10, 10, 11, 0.9) 100%)",
        "gold-kinetic": "linear-gradient(90deg, #D4AF37, #FFD700, #D4AF37)"
      },
      borderRadius: {
        "2xl": "1rem",
        "3xl": "1.5rem",
        "4xl": "2rem",
        "5xl": "2.5rem",
        "6xl": "3.5rem"
      },
      animation: {
        "spin-slow": "spin 8s linear infinite",
        "pulse-slow": "pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite",
        "float": "float 6s ease-in-out infinite",
        "shimmer": "shimmer 8s linear infinite"
      },
      keyframes: {
        float: {
          "0%, 100%": { transform: "translateY(0)" },
          "50%": { transform: "translateY(-10px)" }
        },
        shimmer: {
          "0%": { backgroundPosition: "-200% 0" },
          "100%": { backgroundPosition: "200% 0" }
        }
      }
    }
  },
  plugins: []
};

export default config;
